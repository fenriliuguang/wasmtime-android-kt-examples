package wasi:clocks@0.3.0;

interface monotonic-clock {
  type instant = u64;
  type duration = u64;
  now: func() -> instant;
  resolution: func() -> duration;
  wait-until: async func(when: instant);
  wait-for: async func(when: duration);
}
