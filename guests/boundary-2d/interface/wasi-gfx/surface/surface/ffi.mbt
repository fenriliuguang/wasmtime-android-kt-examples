// Generated by `wit-bindgen` 0.60.0. DO NOT EDIT!

fn wasmImportResourceDropSurface(resource : Int) = "wasi-gfx:surface/surface@0.2.0" "[resource-drop]surface"

fn wasmImportConstructorSurface(p0 : Int, p1 : Int, p2 : Int, p3 : Int) -> Int = "wasi-gfx:surface/surface@0.2.0" "[constructor]surface"

fn wasmImportMethodSurfaceOnFrame(p0 : Int) -> Int = "wasi-gfx:surface/surface@0.2.0" "[method]surface.on-frame"

fn wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Read(handle : Int, buffer_ptr : Int, length : Int) -> Int = "wasi-gfx:surface/surface@0.2.0" "[async-lower][stream-read-0][method]surface.on-frame"
fn wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0CancelRead(handle : Int) -> Int = "wasi-gfx:surface/surface@0.2.0" "[stream-cancel-read-0][method]surface.on-frame"

fn wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0DropReadable(handle : Int) = "wasi-gfx:surface/surface@0.2.0" "[stream-drop-readable-0][method]surface.on-frame"

fn wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Malloc(length : Int) -> Int {
      let ptr = mbt_ffi_malloc(1 * length)
      ptr
}

fn wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Reject(
ptr : Int,
start : Int,
length : Int,
) -> Unit {
      ignore((ptr, start, length))
}

fn wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Lift(ptr : Int) -> FrameEvent {

      FrameEvent::{nothing : (mbt_ffi_load8_u((ptr) + 0) != 0)}
}

fn wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0ListLift(
ptr : Int,
length : Int,
) -> FixedArray[FrameEvent] {

      FixedArray::makei(
      length,
      (index) => {
            let ptr = (ptr) + (index * 1)
            wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Lift(ptr)
      }
      )

}

priv struct WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource {
      handle : Int
      mut closed : Bool
      mut reading : Bool
      mut read_task : Int
      mut read_buffer : Int
      mut read_discarding : Bool
      mut read_cleanup_done : Bool
      read_cleanup : @async-core.CondVar
}

fn WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource::finish_read(
self : WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource,
) -> Unit {
      self.reading = false
      self.read_task = 0
      self.read_buffer = 0
      self.read_discarding = false
      // Keep completion latched until a later read starts so every waiter woken
      // by the broadcast can observe it.
}

async fn WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource::wait_for_read_cleanup(
self : WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource,
) -> Unit noraise {
      @async-core.protect_from_cancel(
      () => {
            while !self.read_cleanup_done {
                  self.read_cleanup.wait()
            }
      },
      resume_on_cancel=true,
      ) catch {
            _ => ()
      }
}

async fn WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource::cancel_active_read(
self : WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource,
) -> Unit noraise {
      if !self.reading {
            return
      }
      if self.read_discarding {
            self.wait_for_read_cleanup()
            return
      }
      self.read_discarding = true
      let progress_ref = Ref(0)
      @async-core.protect_from_cancel(
      () => {
            progress_ref.val = @async-core.cancel_stream_read(
            self.read_task,
            self.handle,
            () => wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0CancelRead(self.handle),
            )
      },
      resume_on_cancel=true,
      ) catch {
            _ => ()
      }
      let progress = progress_ref.val
      if progress > 0 && self.read_buffer != 0 {
            wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Reject(self.read_buffer, 0, progress)
      }
      self.read_cleanup_done = true
      self.read_cleanup.broadcast()
}

async fn WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource::close(
self : WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource,
) -> Unit noraise {
      if self.closed {
            return
      }
      if self.reading {
            self.cancel_active_read()
      }
      self.closed = true
      wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0DropReadable(self.handle)
}

fn WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource::close_sync(
self : WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource,
) -> Unit {
      if self.closed || self.reading {
            return
      }

      self.closed = true
      wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0DropReadable(self.handle)
}

async fn WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource::read(
self : WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource,
count : Int,
) -> FixedArray[FrameEvent]? {
      if self.closed {
            return None
      }
      if count <= 0 {
            return Some([])
      }
      if self.reading {
            raise @async-core.EndpointBusy::Read
      }
      // `Stream.read` promises at most `count`; bound one canonical allocation.
      let read_count = if count < 64 {
            count
      } else {
            64
      }
      let ptr = wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Malloc(read_count)
      let mut owns_buffer = false
      defer {
            if !owns_buffer {
                  mbt_ffi_free(ptr)
            }
      }
      self.reading = true
      self.read_task = @async-core.current_component_task_token()
      self.read_buffer = ptr
      self.read_discarding = false
      self.read_cleanup_done = false
      let (progress, end) = @async-core.suspend_for_stream_read(
      self.handle,
      wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0Read(self.handle, ptr, read_count),
      ) catch {
            err => {
                  if self.read_discarding {
                        self.wait_for_read_cleanup()
                        self.finish_read()
                        return None
                  }
                  if err is @async-core.Cancelled::Cancelled {
                        self.cancel_active_read()
                        if !self.closed {
                              self.closed = true
                              wasmImportWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0DropReadable(self.handle)
                        }
                  }
                  self.finish_read()
                  raise err
            }
      }
      if self.read_discarding {
            self.wait_for_read_cleanup()
            self.finish_read()
            return None
      }
      if progress == 0 {
            self.finish_read()
            if end {
                  self.close()
                  return None
            }
            return Some([])
      }
      let values = wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0ListLift(ptr, progress)
      owns_buffer = false
      self.finish_read()
      if end {
            self.close()
      }
      Some(values)
}

fn wasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamLift(handle : Int) -> @async-core.Stream[FrameEvent] {
      let source = WasmWasiGfxSurfaceSurface020MethodSurfaceOnFrameStream0StreamSource::{
            handle,
            closed: false,
            reading: false,
            read_task: 0,
            read_buffer: 0,
            read_discarding: false,
            read_cleanup_done: false,
            read_cleanup: CondVar(),
      }
      @async-core.Stream::from_source(
      (count) => source.read(count),
      () => source.close(),
      () => source.close_sync(),
      )
}

///|
extern "wasm" fn mbt_ffi_store8(offset : Int, value : Int) =
#|(func (param i32) (param i32) local.get 0 local.get 1 i32.store8)

///|
extern "wasm" fn mbt_ffi_free(position : Int) =
#|(func (param i32) local.get 0 call $moonbit.decref)

///|
extern "wasm" fn mbt_ffi_malloc(size : Int) -> Int =
#|(func (param i32) (result i32) (local i32)
#| local.get 0 i32.const 4 i32.add call $moonbit.gc.malloc
#| local.tee 1 i32.const 0 call $moonbit.init_array8
#| local.get 1)

///|
extern "wasm" fn mbt_ffi_load8_u(offset : Int) -> Int =
#|(func (param i32) (result i32) local.get 0 i32.load8_u)

