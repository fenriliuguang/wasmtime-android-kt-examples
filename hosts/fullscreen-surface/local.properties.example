sdk.dir=/path/to/Android/sdk
# Hosts consume Maven Central android-webgpu:0.1.2. A local runtime checkout
# is not required. To override with includeBuild, restore settings.gradle.kts
# and set wasmtime.android.kt.dir here.
